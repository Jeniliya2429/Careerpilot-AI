# ==============================================================
# CareerPilot AI — backend dependencies
# Verified: all packages below ship prebuilt wheels for
# Python 3.12 on Windows (win_amd64) — no C/C++ compilation,
# no Visual Studio / Build Tools required.
# ==============================================================

# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.19

# ---- Database (SQLAlchemy + Supabase Postgres) ----
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
# ^ "-binary" build ships a prebuilt wheel with libpq bundled in —
#   this is the one that avoids needing C++ Build Tools on Windows.
#   Never swap this for plain "psycopg2" in this project.

# ---- Auth ----
pyjwt==2.10.1
bcrypt==4.2.1
# ^ used directly (not via passlib) — passlib's bcrypt backend has had
#   flaky Windows wheel resolution; raw `bcrypt` package has clean
#   cp312-win_amd64 wheels.

# ---- Supabase Storage client (for resume PDF blobs only; DB is SQLAlchemy) ----
supabase==2.10.0

# ---- AI orchestration ----
langchain==0.3.13
langchain-openai==0.2.14
langgraph==0.2.60
openai==1.58.1
email-validator==2.2.0
# ---- RAG / vector store ----
chromadb==1.5.9

# ^ 0.5.x ships prebuilt wheels (incl. its hnswlib dependency) for
#   cp312-win_amd64 on PyPI — confirmed no local build step on Windows.

# ---- File handling ----
pypdf==5.1.0
reportlab==4.2.5
# both are pure-Python / pre-built-wheel packages, no compilation.

# ---- Testing ----
pytest==8.3.4
pytest-mock==3.14.0
httpx==0.27.2
# ^ pinned to match what supabase==2.10.0 requires (not 0.28.x) — FastAPI's
#   TestClient works fine on this version too.
